import React from "react";
import { IonHeader, IonTitle, IonToolbar } from "@ionic/react";

const Header = () => {
  return (
    <IonHeader>
      <IonToolbar>
        <IonTitle>Test Capacitor App</IonTitle>
      </IonToolbar>
    </IonHeader>
  );
};

export default Header;
